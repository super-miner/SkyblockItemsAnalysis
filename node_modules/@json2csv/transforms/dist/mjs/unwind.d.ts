import type Transform from './Transform.js';
export interface UnwindOptions {
    paths?: ReadonlyArray<string>;
    blankOut?: boolean;
}
/**
 * Performs the unwind recursively in specified sequence
 *
 * @param {String[]} unwindPaths The paths as strings to be used to deconstruct the array
 * @returns {Object => Array} Array of objects containing all rows after unwind of chosen paths
 */
export default function unwind<I extends object = object, O extends object = object>(opts?: UnwindOptions): Transform<I, O>;
//# sourceMappingURL=unwind.d.ts.map